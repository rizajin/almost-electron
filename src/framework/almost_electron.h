#ifndef ALMOST_ELECTRON_H
#define ALMOST_ELECTRON_H

__declspec(dllexport) double Test(double a, double b);
__declspec(dllexport) void Window(/* lol, maybe it could be parameterized but as this is just a poc.. */);

#endif
